// Use when you need order

// Search: O(n)
// Access: O(1)
// Insertion/removal: variable!

// Inserting and removing in places other than end of array causes reindex cascade on all items that come later in array: O(n)

// push/pop: O(1)
// shift/unshift/concat/slice/splice/forEach/map/reduce/filter: O(n)
// sort: O(n * log n)

